"use client";

import { FieldProps } from "../Field";

export default function TitleFieldEdit(props: FieldProps) {

  return (
    <div className="flex items-center gap-1" id={props?.label}>
    </div>
  );
}
